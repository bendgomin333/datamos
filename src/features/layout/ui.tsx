import { Header } from "../header"
import { Routing } from "../routing"

export const Layout = () => {
    return (
        <>
            <main>
                <Routing />
            </main>
        </>
    )
}